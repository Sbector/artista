export type Props = {
    params: {
        slug: string[]
    }
}

export type Obra = {
    title: string
    year: string
    descripttion: string
}

export type Section = {
        label: string
        route: string
    }